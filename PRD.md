# 自主學習導航 Web 應用 — 產品需求文件（PRD）

**狀態：** 待確認後作為開發依據  
**版本：** 1.1（MVP）  
**日期：** 2026-08-16  
**介面語言：** 繁體中文

本文檔彙整已確認的產品定位、頁面結構、資料模型、聯動關係、雲端架構與驗收標準。開發時以本文為準。生成後先不寫功能代碼，待本文件確認。

每個模組必須有符合自身場景的專屬互動與 UI 狀態，不能只是通用 Todo List 換名字。

---

## 1. 產品目標與使用場景

### 1.1 產品目標

讓朋友在不同裝置上**連線共學**：用 Google 帳號進入同一個群組，一起管理學習目標、用 AI 把目標拆成關卡、用低壓打卡推進地圖，並共同養一隻群組寵物。

四大目標必須同時成立：

| 目標 | 含義 |
|------|------|
| 真人群組連線 | 真實好友、第三方登入、雲端資料、線上部署；不是單機假隊友 |
| 多目標管理 | 一人可加入多群組；每群獨立目標、地圖、寵物、動態 |
| AI 拆解 | Gemini 免費層把目標變成階段／每日任務／里程碑頭目；常見目標走快取；失敗走範本 |
| 低壓寵物共養 | 每日任務一鍵打卡即可餵寵物；只有頭目／里程碑才要上傳憑據 |

### 1.2 目標使用者

想和朋友一起推進考試或技能學習的自學者。手機與電腦瀏覽器皆可使用。

### 1.3 核心使用場景

1. **開團：** A 用 Google 登入、建立群組、把邀請碼傳給 B；B 加入後看到同一隻寵物與同一張地圖。
2. **拆目標：** 在首頁輸入「學測英文」或「學會吉他」，一鍵拆解，預覽關卡樹後套用到本群或另開新群。
3. **今日共學：** 打開首頁看今日路線與隊友完成圈；一般關卡低壓打卡；頭目上傳照片或檔案。
4. **一起過關：** 群主若開啟審核，隊友在動態牆按通過後，關卡才亮、寵物才加較多經驗。
5. **切群組：** 頂欄切到另一個群，地圖、寵物、動態、今日路線全部換成該群資料，互不混用。

### 1.4 已確認決策

| 議題 | 決策 |
|------|------|
| 共學方式 | 真人人連線：Google 登入、雲端資料庫、線上部署、憑據上傳、群組動態牆 |
| 任務完成 | 每日任務低壓打卡；里程碑／頭目必須上傳憑據 |
| 審核 | 群主可開關。關閉（預設）＝上傳即通過。開啟＝任一隊友通過後才過關並加經驗 |
| AI | Gemini Free Tier ＋ 雲端目標快取 ＋ 無 API／失敗時範本後備 |
| 動態牆 | 僅目前群組內，不是公開社群 |
| 加入群組 | 邀請碼 |
| 導覽 | 登入頁 ＋ 應用外殼；頂欄切群組；四主分頁 |

---

## 2. 整體頁面框架與導航結構

### 2.1 全站地圖

```
登入頁
  └─ 應用外殼（須先登入）
        頂欄：群組切換 ／ 寵物縮圖 ／ 使用者頭像
        主分頁：
          ├─ 首頁
          ├─ 關卡地圖
          ├─ 寵物小隊
          └─ 動態牆
        浮層／子頁：
          建立群組、加入群組、拆解預覽、
          每日打卡、憑據上傳、隊友審核、群組設定
```

### 2.2 登入頁

- 一句產品說明 ＋ **Google 登入**按鈕。
- 未登入不可進入應用外殼。
- 登入成功後：
  - 無任何群組 → 空狀態「尚未加入任何共學群」（建立／輸入邀請碼）。
  - 有群組 → 進入**上次選定群組**的首頁；若無紀錄則進入最近加入的群組。

### 2.3 應用外殼

所有主分頁共用同一外殼。外殼的「目前群組 ID」是全域上下文：首頁、地圖、寵物、動態、設定都只讀這個群組的資料。

**頂欄**

- **群組切換：** 不是普通文字下拉。每一列是一張群組卡：寵物頭、群組目標名、未讀動態數。列表底部固定「建立群組」「加入群組」。
- **寵物縮圖：** 目前群組寵物的等級小圖；點擊進入寵物小隊頁。
- **使用者頭像：** 登出、進入目前群組的設定。

### 2.4 響應式導覽

| 裝置 | 主分頁位置 | 地圖 |
|------|------------|------|
| 手機（優先） | 底部四個分頁：首頁／地圖／寵物／動態 | 直向樹，可捲動 |
| 電腦 | 左側欄四個分頁 | 可加寬，允許由左到右展開 |

切換分頁不改變目前群組。切換群組時，四個分頁的資料一起換，停留在當前分頁。

---

## 3. 首頁的內容結構

首頁是**指揮台**，不是待辦清單。由上到下四個區塊，皆綁定「目前群組」。

### 3.1 目標輸入區

- 大輸入框文案：「你想學什麼？」
- 主按鈕：「一鍵拆解」
- 送出後進入**拆解預覽浮層**，禁止直接跳到空白任務列表。
- 狀態：
  - **拆解中：** 文案改為「正在把目標拆成關卡」，階段名稱一節一節出現。
  - **快取命中：** 「這是常見目標，已套用精選拆解」。
  - **API 失敗／無金鑰：** 「先用範本幫你排一條路線」，仍可預覽與創建。

### 3.2 寵物看板

- 顯示目前群組寵物外觀、名稱、等級、經驗條。
- 點擊整塊看板 → 寵物小隊頁。
- 當本群有人打卡或過關時，經驗條短暫跳動（資料來源同寵物模組）。

### 3.3 隊友今日圈

- 本群成員頭像圍成一圈（含自己）。
- 每人頭像外圈＝**今日路線任務完成比例**（今日應做題數／已完成題數）。
- 點頭像 → 只看該成員今日進度摘要，不能私訊。

### 3.4 今日路線短路徑

- 從目前群組關卡樹抽出**今天 3～5 個可做節點**（規則見 6.2）。
- 畫成短路徑，不是 checkbox 列表。
- 一般任務＝可點亮小點；頭目／里程碑＝較大關卡石，帶「需憑據」標記。
- 點節點 → 與關卡地圖相同的打卡卡或上傳卡。
- **無今日任務：** 「今天地圖上沒有新關卡」，引導去關卡地圖或輸入新目標。

---

## 4. 每個模組的用途

| 模組 | 用途 | 不是什麼 |
|------|------|----------|
| **首頁** | 一鍵拆解新計畫；一眼看到本群寵物、隊友今日、今天該走哪幾關 | 不是全量任務清單 |
| **關卡地圖** | 以可互動關卡樹展現本群目標進度；打卡／上傳憑據推進關卡 | 不是摺疊式 Todo |
| **寵物小隊** | 展示共養寵物成長、本週餵養貢獻、團隊成就 | 不是個人資料頁或羞辱式排行 |
| **群組動態牆** | 本群共學片刻：打卡、憑據、審核、升級、成就、新成員；可按讚與審核 | 不是公開社群、不是留言串 |
| **群組設定** | 邀請碼、審核開關、退出／解散 | 不是全站帳號中心 |
| **拆解預覽**（浮層） | 確認／微調 AI 或範本產出的樹，再寫入群組 | 不是直接存檔的黑箱 |

---

## 5. 各模組第一版資料模型與操作流程

以下為 Firestore 邏輯模型。實作時集合名稱可加前綴，但欄位與關係不可少。時間一律存 UTC，畫面用本地時區算「今天」。

### 5.0 共用列舉

**節點類型 `nodeType`：** `daily`（一般每日任務）｜ `milestone`（里程碑）｜ `boss`（關卡頭目）

**節點進度 `progressStatus`：** `locked`｜ `available`｜ `pending_upload`｜ `pending_review`｜ `done`｜ `need_resubmit`

**動態類型 `feedType`：** `checkin`｜ `evidence_submitted`｜ `cleared`｜ `pending_review`｜ `pet_level_up`｜ `achievement`｜ `member_joined`

**經驗常數（第一版固定，便於驗收）**

| 事件 | 寵物經驗 | 成員貢獻 |
|------|----------|----------|
| 每日任務打卡完成 | ＋10 | ＋10 |
| 里程碑／頭目過關 | ＋50 | ＋50 |
| 僅上傳尚未通過 | 0 | 0 |

**寵物升級：** 升到第 N 級需要累計 `N × 100` 經驗（Lv2＝100，Lv3＝300，Lv4＝600…）。第一版外觀至少 3 階段（蛋／幼體／成長），依等級切換。

---

### 5.1 帳號與應用外殼

**資料**

```
users/{uid}
  displayName, photoURL, email
  lastSelectedGroupId
  createdAt

groups/{groupId}/members/{uid}
  role: owner | member
  joinedAt
  weeklyContribution    // 本週餵養（可週一重置或滾動 7 日）
  totalContribution
```

**流程**

1. Google 登入 → 若無 `users/{uid}` 則建立。
2. 讀取自己加入的群組列表（members 反查或 `users/{uid}/memberships`）。
3. 選定 `lastSelectedGroupId` 作為外殼上下文。
4. 登出清除本機會話，不刪雲端資料。

---

### 5.2 群組設定模組

**資料**

```
groups/{groupId}
  name
  inviteCode              // 唯一、可複製；群主可重新產生
  ownerId
  reviewEnabled           // 預設 false
  currentPlanId           // 目前進行中的地圖；舊計畫保留但不推進
  petId
  createdAt
```

**操作流程**

| 操作 | 誰 | 結果 |
|------|----|------|
| 建立群組 | 登入者 | 新群、邀請碼、建立者為 owner、預設寵物 Lv1／0 經驗、reviewEnabled=false；可同時套用一棵新地圖 |
| 加入群組 | 持邀請碼者 | 寫入 members；動態牆 `member_joined`；不可重複加入 |
| 複製邀請碼 | 成員 | 複製到剪貼簿 |
| 重新產生邀請碼 | 僅群主 | 舊碼失效 |
| 開關審核 | 僅群主 | 只影響之後的頭目上傳；已通過的關卡不回溯 |
| 退出 | 非群主成員 | 移出 members；寵物經驗不扣回 |
| 解散 | 僅群主，需二次確認 | 群組不再可進入（資料可保留或軟刪，第一版採軟刪 `archived: true`） |

---

### 5.3 首頁 ＋ AI 拆解模組

**資料**

```
goalCache/{cacheKey}
  sourceGoal              // 原始常用字串
  normalizedKey           // 去前後空白、小寫、壓縮空白
  tree                    // 階段／節點結構 JSON
  source: gemini | template
  createdAt
  hitCount

plans/{planId}
  groupId
  goalText
  source: gemini | cache | template
  cacheKey
  tree                    // 確認後的結構（可含使用者改名／刪節點）
  status: active | archived
  createdBy
  createdAt

// tree 節點最小欄位
node: {
  id, parentId, title, nodeType, order,
  unlockRule: "previous_sibling_done"
}
```

**正規化：** `trim` → 連續空白變單一空白 → 英文小寫。相同 `normalizedKey` 視為同一常見目標。

**拆解流程**

1. 使用者在首頁送出 `goalText`。
2. 算 `normalizedKey`，查 `goalCache`。
3. **命中：** 用快取樹進預覽；UI「常見目標，已套用精選拆解」；`hitCount++`。不呼叫 Gemini。
4. **未命中：** 呼叫 Gemini Free Tier。
   - 成功：寫入 `goalCache`（source=gemini）並進預覽。
   - 無金鑰或失敗：用內建範本產樹（至少 2 個階段、每階段含 daily 與一個 milestone 或 boss），UI「先用範本幫你排一條路線」。**範本結果不寫入快取**（避免污染常見目標）。
5. 預覽：可改節點名稱、刪節點；不可改寵物。
6. 確認時二選一：
   - **套用目前群組：** 若已有 `currentPlanId`，二次確認後將舊 plan 設 `archived`，新 plan 設 `active` 並更新 `currentPlanId`。舊進度保留在舊 plan 的 progress 中，不再推進。
   - **開新群組養這棵樹：** 建立新群＋新 plan＋新寵物，並切換到新群。
7. 為群內每位現有成員初始化該 plan 的進度（第一關 `available`，其餘依依賴 `locked`）。

**Gemini 產出必須能對應到 `nodeType`。** 若模型漏標，後端／客戶端用規則補：每階段最後一個節點視為 `milestone` 或 `boss`，其餘為 `daily`。

---

### 5.4 關卡地圖模組

**資料**

```
plans/{planId}/nodes/{nodeId}     // 或內嵌於 plan.tree
  title, nodeType, parentId, order

progress/{planId}_{uid}_{nodeId}  // 或 groups/{id}/progress/{uid}/nodes/{nodeId}
  status
  note                    // 每日打卡選填一句話
  completedAt
  evidenceId

evidence/{evidenceId}
  groupId, planId, nodeId, uid
  fileUrl                 // Firebase Storage
  contentType             // jpg / png / pdf
  status: pending_review | approved | need_resubmit
  reviewedBy, reviewedAt
  createdAt
```

**每日任務打卡流程**

1. 節點必須 `available`。
2. 使用者點「完成打卡」（可選一句話），不必上傳。
3. 狀態 → `done`；寫入寵物經驗 ＋10、貢獻 ＋10；解鎖下一關；動態 `checkin`。
4. 首頁今日路線與隊友圈立即用同一筆 progress 重算。

**頭目／里程碑流程**

1. 節點必須 `available`，主視覺為上傳區（jpg／png／pdf）。
2. 上傳成功寫 `evidence`。
3. 若 `groups.reviewEnabled === false`：evidence 直接 `approved`，節點 `done`，寵物 ＋50，動態 `cleared`。
4. 若 `reviewEnabled === true`：evidence `pending_review`，節點 `pending_review`，動態 `pending_review`，**此時不加經驗**。
5. 任一**其他成員**按通過：evidence `approved`，節點 `done`，寵物 ＋50，動態 `cleared`。上傳者不能審自己。
6. 第一版審核主按鈕為「通過」。狀態列舉保留 `need_resubmit` 供後續，MVP 可不做「請再補」完整退回流程。

**解鎖規則：** 同一階段依 `order`，上一節點 `done` 才能讓下一節點 `available`。未解鎖點擊只晃動並提示「先打通上一關」。

---

### 5.5 寵物小隊模組

**資料**

```
pets/{petId}   // 或內嵌 groups/{groupId}.pet
  groupId
  name
  level
  xp
  stage           // egg | hatchling | grown（由 level 推導亦可）
  updatedAt

achievements/{groupId}_{achievementId}
  unlockedAt
  unlockedByEvent
```

**第一版團隊成就（固定三個，可檢查）**

| ID | 條件 |
|----|------|
| `first_feed` | 本群任一成員完成第一個每日打卡 |
| `first_boss` | 本群任一成員通過第一個頭目／里程碑 |
| `level_3` | 寵物達到 Lv3 |

**流程：** 任何「完成並加經驗」的寫入必須同一交易（或等效順序）更新：progress → pet.xp／level → member 貢獻 → 若達成就則寫成就 → feed。寵物頁讀同一份 pet 與本週貢獻。

---

### 5.6 群組動態牆模組

**資料**

```
groups/{groupId}/feed/{feedId}
  type
  uid
  planId, nodeId, evidenceId, petLevel, achievementId
  likeUids[]
  createdAt

users/{uid}/groupMeta/{groupId}
  lastReadFeedAt          // 頂欄未讀數用
```

**流程**

- 新事件 prepend 到本群 feed。
- 篩選：全部／過關（`cleared`）／寵物（`pet_level_up`、`achievement`）。
- 按讚：把自己 uid 加入或移出 `likeUids`。
- `pending_review` 卡片顯示憑據縮圖；非作者的本群成員可按「通過」（呼叫與地圖相同的審核流程）。
- 進入動態牆或外殼標記已讀時更新 `lastReadFeedAt`。

---

## 6. 首頁、今日待辦與各模組的資料串接

所有主畫面都訂閱「目前群組」的同一份雲端資料，禁止各頁各做一份互不同步的本地假資料。

```
                    ┌─ users.lastSelectedGroupId ─┐
                    ▼                             │
              groups/{id}                         │
           plan + pet + members                   │
                    │                             │
     ┌──────────────┼──────────────┬─────────┐    │
     ▼              ▼              ▼         ▼    │
   首頁          關卡地圖        寵物小隊   動態牆 │
     │              │              │         │    │
     └──────────────┴──────────────┴─────────┘    │
                    │ 寫入 progress / pet / feed   │
                    └─────────────────────────────┘
```

### 6.1 聯動表

| 動作 | 關卡地圖 | 首頁今日路線 | 首頁隊友圈 | 寵物看板／寵物頁 | 動態牆 | 頂欄 |
|------|----------|--------------|------------|------------------|--------|------|
| 切換群組 | 換樹 | 重算今日 3～5 關 | 換成該群成員 | 換寵物 | 換該群 feed | 換群組卡與寵物縮圖 |
| 每日打卡 | 節點亮 | 該點完成 | 自己外圈增加 | XP＋10、條跳動 | `checkin` | 未讀＋1（他人） |
| 頭目上傳（無審核） | 過關 | 該石完成 | 外圈增加 | XP＋50 | `cleared` | 未讀 |
| 頭目上傳（有審核） | 審核中 | 該石顯示審核中 | 外圈不變 | 不變 | `pending_review` | 未讀 |
| 隊友按通過 | 亮關 | 上傳者該石完成 | 上傳者外圈增加 | XP＋50 | `cleared` | 未讀 |
| 套用新地圖 | 新樹 | 依新樹重算今日 | 進度歸零重算 | 寵物不重置 | 可發系統向動態（可選） | — |
| 新成員加入 | 為其初始化進度 | 圈上多一頭像 | 多一頭像 | 不變 | `member_joined` | 未讀 |

寵物經驗與貢獻**只在節點變成 `done` 時**增加一次，禁止重複計分。

### 6.2 「今日路線」抽題規則

從 `currentPlan` 中取目前使用者狀態為 `available` 或 `pending_upload` 或 `pending_review` 的節點，依地圖 `order` 排序，最多 5 個、至少嘗試 3 個。

- 若可做節點不足 3 個，有幾個顯示幾個。
- 若 0 個：空狀態「今天地圖上沒有新關卡」。
- 頭目即使是唯一可做節點，也要出現在路線上並標「需憑據」。
- 今日完成度（隊友圈）＝ `今天 00:00–24:00（本地）內變成 done 的節點數` ／ `該成員今日路線應做題數`。分母用「當日第一次進入首頁時鎖住的路線快照」或「當下可做＋今日已完成」；第一版採用較簡單者：**分母＝目前可做（含審核中）＋今日已完成的節點數，上限 5；分子＝其中已 `done` 的數量**。

### 6.3 拆解與首頁

拆解預覽確認後：`currentPlanId` 更新 → 地圖換樹 → 今日路線立刻改抽新樹 → 隊友圈分母重算。寵物經驗不因換地圖而清零。

---

## 7. 雲端資料庫、Gemini 快取、離線與本機同步

### 7.1 雲端資料庫架構（Firebase）

| 服務 | 用途 |
|------|------|
| Firebase Auth | Google 登入 |
| Cloud Firestore | 使用者、群組、計畫樹、進度、寵物、成就、動態、目標快取 |
| Firebase Storage | 憑據檔（jpg／png／pdf） |
| Firebase Hosting（或同等） | 線上部署，好友用網址進入 |

**建議集合**

```
users/{uid}
users/{uid}/memberships/{groupId}
groups/{groupId}
groups/{groupId}/members/{uid}
groups/{groupId}/feed/{feedId}
pets/{petId}
plans/{planId}
progress/{id}
evidence/{id}
goalCache/{normalizedKey}
achievements/{id}
```

**權限原則（驗收時必須有規則，不可全開）**

- 未登入不能讀寫業務資料。
- 只能讀自己加入的群組。
- 進度、打卡、上傳：僅本人可為自己的節點寫入；審核通過：僅同群非作者可寫。
- `reviewEnabled`、解散、重發邀請碼：僅 owner。
- `goalCache`：登入者可讀；寫入僅由拆解成功路徑寫入（Cloud Function 或受控 client 寫入，需防濫用：同一使用者短時間重複寫同一 key 直接命中）。
- Storage：僅同群成員可讀該群憑據；僅上傳者可寫自己的憑據路徑。

**即時：** 目前群組的 pet、progress、feed、members 使用即時監聽（或等效輪詢，間隔須短到隊友打卡後數秒內首頁／寵物看得到）。雲端為多人資料的**唯一真相來源**。

### 7.2 Gemini API 與快取機制

```
送出目標
  → 正規化 normalizedKey
  → 讀 goalCache/{key}
       ├─ 命中：回傳 tree，hitCount+1，不呼叫 Gemini
       └─ 未命中：呼叫 Gemini Free Tier
              ├─ 成功：寫 goalCache（source=gemini），回傳 tree
              └─ 失敗／無金鑰：template tree，不寫 cache
  → 使用者預覽編輯
  → 確認後才寫 plans（source 標記 gemini | cache | template）
```

- API 金鑰只放伺服端或建置環境變數，不可寫進前端公開 repo。
- 免費層保護：先快取再請求；相同常見目標全站共用快取。
- 快取內容是「建議樹」，群組實際使用的是確認後的 `plans` 副本，互不覆蓋。
- 第一版不做語意近似比對（不把「學測英文」與「準備學測英文科」強行視為同一 key，除非正規化後字串完全相同）。

### 7.3 離線／本機狀態同步要求

第一版不是「僅 LocalStorage 的單機 App」，但必須處理短暫離線與重新整理。

| 項目 | 要求 |
|------|------|
| 重新整理／重開分頁 | 已登入則恢復 `lastSelectedGroupId` 與該群畫面，不丟雲端資料 |
| 本機記住的狀態 | 目前群組 ID、目前主分頁、動態已讀時間；可用 Firestore 的 `lastSelectedGroupId` ＋ 本機 session |
| Firestore 離線快取 | 啟用 persistence。已載入過的目前群組資料，短暫斷網仍可看地圖／寵物／今日路線 |
| 離線寫入 | 每日打卡允許離線排隊，連線後同步。頭目上傳檔案在離線時應明確提示「請連上網再上傳」，第一版不強制離線傳檔 |
| 衝突 | 同一節點以雲端最終狀態為準；已 `done` 的節點不可因離線重送再加一次經驗（寫入須冪等：同一 uid+nodeId 只能完成一次） |
| 多群組 | 本機不得把 A 群進度寫進 B 群 |
| 登出 | 清除本機會話與離線快取中的帳號上下文 |

斷網時：可瀏覽已快取內容；打卡按鈕可進入「待同步」；外殼顯示不刺眼的「離線中，連上後會同步」。禁止在無快取時顯示空白當資料遺失。

---

## 8. 第一版（MVP）必須完成的功能清單

1. Google 登入、登出；未登入不可進外殼。
2. 建立群組、邀請碼加入、頂欄群組卡切換；每群獨立目標／地圖／寵物／動態。
3. 響應式外殼：手機底欄、電腦側欄；頂欄群組切換、寵物縮圖、頭像。
4. 無群組空狀態：建立或加入。
5. 首頁四區塊：目標輸入、寵物看板、隊友今日圈、今日路線（3～5 關短路徑）。
6. 一鍵拆解：Gemini Free Tier；快取命中不重複請求；失敗／無金鑰走範本；預覽可改名／刪節點。
7. 預覽確認：套用本群（舊地圖封存）或開新群養樹。
8. 關卡地圖：樹狀節點、解鎖依賴、目前關卡可點、未解鎖不可跳關。
9. 每日任務低壓打卡（選填一句話），不加照片。
10. 頭目／里程碑必傳 jpg／png／pdf。
11. 群主開關「頭目需隊友審核」；關＝上傳即通過；開＝任一其他隊友通過後才過關加經驗。
12. 寵物經驗／等級／至少三階段外觀；本週餵養貢獻；三個團隊成就。
13. 群組動態牆：指定卡片類型、篩選（全部／過關／寵物）、按讚；審核中卡片可通過。
14. 群組設定：複製／重發邀請碼、審核開關、退出、群主解散確認。
15. 打卡／過關後首頁、地圖、寵物、動態、頂欄未讀聯動。
16. Firestore 為真相來源；重新整理不丟資料；短暫離線可看已快取內容、打卡冪等同步。
17. Storage 上傳憑據；線上部署後朋友可用網址登入加入。
18. 繁中文案；各模組專屬 UI（指揮台／地圖／共養／動態），禁止共用一個列表改標題。

---

## 9. 第一版暫時不做（避免過度開發）

- 公開搜尋群組或陌生人、追蹤、私訊、留言串、全站動態、跨群排行榜
- 每日任務拍照打卡或上傳憑據
- 審核需湊滿 N 人、多管理員／助教職級、完整「請再補」退回流程
- AI 家教聊天、改作業、語音、語意近似快取
- 直播共學、行事曆同步、推播通知、付費、電子郵件密碼登入（Google 即可）
- 原生 App、多語言、後台 CMS
- 離線上傳大檔、多寵物、個人寵物（每群僅一隻共用）
- 換地圖時重置寵物經驗

---

## 10. 驗收標準（Acceptance Criteria）

以下每條都必須能實際操作勾選。未勾選視為第一版未完成。

### 10.1 帳號與導覽

- [ ] 未登入只看得到登入頁；Google 登入成功進入外殼。
- [ ] 登出後需重新登入才能看群組資料。
- [ ] 手機有底部四個分頁；電腦有側欄四個分頁；分頁為首頁、關卡地圖、寵物小隊、動態牆。
- [ ] 頂欄可切換群組；切換後四個分頁資料都變成新群組，舊群資料不出現。
- [ ] 群組切換列是群組卡（寵物頭、目標名、未讀），不是純文字 select。
- [ ] 無群組時顯示建立／加入，不能進入空的地圖裝資料。

### 10.2 群組

- [ ] 建立群組後得到可複製的邀請碼；另一 Google 帳號輸入該碼可加入，雙方看到同一寵物與同一地圖。
- [ ] 錯誤邀請碼無法加入，並有白話提示。
- [ ] 群主可重發邀請碼，舊碼無法再加入。
- [ ] 群主可開關「頭目需隊友審核」，預設為關。
- [ ] 成員可退出；群主解散需二次確認，解散後成員無法再進該群。

### 10.3 首頁結構

- [ ] 首頁由上到下為：目標輸入區、寵物看板、隊友今日圈、今日路線，不是單一 Todo 列表。
- [ ] 輸入目標按一鍵拆解會開預覽，不會直接出現空白清單。
- [ ] 寵物看板顯示等級與經驗條；點擊進入寵物小隊。
- [ ] 隊友圈人數＝本群成員數；外圈比例隨今日完成變化。
- [ ] 今日路線為短路徑，節點數 1～5（有可做關卡時）；頭目節點有「需憑據」標記。
- [ ] 無任何可做關卡時出現「今天地圖上沒有新關卡」而不是空白崩潰。

### 10.4 AI 拆解與快取

- [ ] 有金鑰且快取未命中時，預覽樹來自 Gemini，且 `plans.source` 為 `gemini` 或確認後可追溯。
- [ ] 兩個使用者（或兩次）輸入正規化後相同的目標，第二次不重複消耗 Gemini（`goalCache.hitCount` 增加或等效紀錄）。
- [ ] 關掉金鑰或模擬 API 失敗時，仍出現範本樹與「先用範本幫你排一條路線」，且可確認創建。
- [ ] 範本結果不寫入 `goalCache`。
- [ ] 預覽可修改節點名稱、刪除節點後再確認，寫入的 plan 為修改後版本。
- [ ] 可選「套用目前群組」或「開新群組」；套用已有地圖時有二次確認；舊 plan 封存、新 plan 成為 current。
- [ ] 換地圖後寵物經驗不被清零。

### 10.5 關卡地圖與兩種任務

- [ ] 地圖是可點的樹（完成發亮、目前可點、未解鎖為霧），不是同一套清單元件改標題。
- [ ] 未打通上一關時，下一關不可完成。
- [ ] 每日任務不出現必填上傳；一鍵打卡後該節點完成。
- [ ] 頭目／里程碑無檔案時不能過關；上傳 jpg／png／pdf 後才進入通過或審核中。
- [ ] 審核關閉：上傳後該關立即 `done`，寵物 ＋50。
- [ ] 審核開啟：上傳後為審核中，寵物經驗不變；另一位隊友按通過後才 `done` 且 ＋50。
- [ ] 上傳者不能通過自己的憑據。
- [ ] 同一關重複打卡不會重複加經驗。

### 10.6 寵物與成就

- [ ] 每群只有一隻共用寵物；A 群升級不影響 B 群寵物。
- [ ] 每日完成 ＋10 XP，頭目通過 ＋50 XP；達到門檻時等級增加，外觀至少分三階段。
- [ ] 寵物頁顯示本週餵養（成員貢獻），完成任務者的貢獻會增加。
- [ ] 達成 `first_feed`、`first_boss`、`level_3` 時成就解鎖，動態牆出現對應卡片。
- [ ] 隊友打卡時，若正在寵物頁或首頁看板，經驗條會更新（即時或數秒內）。

### 10.7 動態牆

- [ ] 只顯示目前群組事件；切群後看不到另一群動態。
- [ ] 至少涵蓋：打卡、憑據／過關、審核中、寵物升級、成就、新成員。
- [ ] 可篩選全部／過關／寵物。
- [ ] 可對動態按讚；再按一次取消。
- [ ] 審核中的憑據卡對其他成員顯示「通過」，通過後地圖與寵物依 10.5 變化。
- [ ] 沒有留言、追蹤、私訊入口。

### 10.8 聯動

- [ ] 在首頁今日路線打卡，地圖對應節點同步完成。
- [ ] 在地圖打卡，首頁路線與隊友圈同步更新。
- [ ] 過關後動態牆新增一則，頂欄該群未讀增加（其他成員視角）。
- [ ] 換群組後今日路線不會出現上一群的關卡名稱。

### 10.9 雲端、離線、部署

- [ ] 重新整理或關閉再開，同一帳號仍在原群組，進度與寵物仍在。
- [ ] 兩台裝置同一帳號資料一致；兩位成員互相看得到對方新完成的關卡與寵物 XP。
- [ ] Firestore 安全規則不允許未登入讀寫；不允許非成員讀該群 feed／progress。
- [ ] 短暫斷網時，已開啟過的目前群組仍可看見快取畫面，並有離線提示。
- [ ] 離線打卡連線後只生效一次（冪等）。
- [ ] 離線時上傳憑據有「請連上網」提示，不會假裝成功。
- [ ] 網站已部署，第二人可用網址 Google 登入並用邀請碼加入。
- [ ] Gemini 金鑰未出現在前端公開原始碼。

### 10.10 範圍控制

- [ ] 每日任務流程中沒有強制拍照。
- [ ] 沒有公開搜尋使用者／群組、私訊、留言串。
- [ ] 沒有 AI 聊天家教入口。

---

## 11. 建議開發順序

1. 專案初始化、Firebase Auth／Firestore／Storage、環境變數、登入頁與外殼。
2. 群組建立／加入／切換、安全規則、本機選定群組持久化。
3. 拆解：快取 → Gemini → 範本；預覽浮層；寫入 plan。
4. 關卡地圖、進度、每日打卡、憑據上傳、審核開關。
5. 寵物 XP／等級／貢獻／三成就。
6. 動態牆、按讚、未讀。
7. 首頁四區塊與第 6 節聯動。
8. 離線 persistence、冪等、部署、依第 10 節逐條驗收。

---

## 12. 設計原則

1. 各模組專屬互動，禁止一套列表改標題交差。
2. 低壓：每日一鍵完成；頭目才要憑據。
3. 共學感來自同群寵物、隊友圈、動態牆，不是公開社群。
4. AI 失敗不能卡住創建；範本必須可走完開圖流程。
5. 免費層靠快取保護；常見目標先查再請求。
6. 雲端為多人真相來源；本機只做快取、選定群組與離線排隊。
7. 文案繁體中文，錯誤提示避免技術術語。

---

*變更範圍時請更新版本號、第 1.4 節決策表，以及第 8～10 節清單。*
